- var pageTitle = site.name + ' » ' + page.title

head
	meta(charset='utf-8')
	meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
	meta(name='viewport', content='width=device-width, initial-scale=1.0, shrink-to-fit=no')

	link(rel='preload', as='style', href='https://fonts.googleapis.com/css?family=Pacifico&text=Craze')
	link(rel='preload', as='style', href='css/challenge' + (site.minify ? '.min' : '') + '.css')
	link(rel='preload', as='script', href='js/challenge' + (site.minify ? '.min' : '') + '.js')
	link(rel='preload', as='script', href=site.fontAwesomeUrl)

	title= pageTitle

	link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Pacifico&text=Craze')
	link(rel='stylesheet', href='css/challenge' + (site.minify ? '.min' : '') + '.css')

	script.
		var tagHtml = document.getElementsByTagName('html')[0];
		tagHtml.className = tagHtml.className.replace('no-js', 'js');
